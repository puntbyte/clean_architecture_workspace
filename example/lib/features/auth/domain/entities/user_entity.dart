// example/lib/features/auth/domain/entities/user_entity.dart

// A compliant entity.
// Its name `UserEntity` matches the `{{name}}Entity` convention.
class UserEntity {
  final String id;
  final String name;

  const UserEntity({required this.id, required this.name});
}
